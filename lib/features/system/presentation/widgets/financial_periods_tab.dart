import 'package:flutter/material.dart';

class FinancialPeriodsTab extends StatelessWidget {
  const FinancialPeriodsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('تبويب الفترات المالية'));
  }
}
