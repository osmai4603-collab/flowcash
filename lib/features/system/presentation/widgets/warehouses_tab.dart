import 'package:flutter/material.dart';

class WarehousesTab extends StatelessWidget {
  const WarehousesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('تبويب المستودعات'));
  }
}
