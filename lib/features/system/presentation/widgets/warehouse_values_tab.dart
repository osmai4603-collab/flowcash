import 'package:flutter/material.dart';

class WarehouseValuesTab extends StatelessWidget {
  const WarehouseValuesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('تبويب قيم المستودعات'));
  }
}
